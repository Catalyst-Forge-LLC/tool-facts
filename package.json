{
  "name": "tool-facts",
  "private": true,
  "scripts": {
    "ship": "wrangler pages deploy site --project-name=tool-facts"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "workerd"]
  }
}
